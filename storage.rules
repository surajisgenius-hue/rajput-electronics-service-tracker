rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /service-record-images/{trackingId}/{fileName} {
      allow read, write, delete: if request.auth != null;
    }
  }
}
